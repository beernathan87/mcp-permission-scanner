# Changelog

## 0.1.0 - 2026-09-14

First release: capability + dangerous-combination + description-injection analysis for MCP servers (stdio and HTTP/SSE) and static tool documents, explainable risk score, snapshot diffs with CI exit codes, config discovery for Claude Desktop/Claude Code/Cursor/Windsurf/VS Code/Codex.

Production pass (2026-09-13): Windows launch of `npx`/`uvx`/`.cmd` servers fixed (cross-spawn), POSIX process-group kill so hostile servers cannot leave grandchildren behind, tests and fixtures removed from the npm tarball, hostile-server regression tests, CI on Ubuntu + Windows from the packed tarball, third-party notices.
