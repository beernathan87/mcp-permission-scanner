{
  "name": "mcp-permission-scanner",
  "version": "0.1.0",
  "description": "Security scanner for MCP servers and tool definitions: capabilities, dangerous combinations, description injection, risk score, and version diffs.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "bin": {
    "mcp-scan": "src/cli.js"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "smol-toml": "^1.3.1",
    "zod": "^4.6.4"
  }
}
